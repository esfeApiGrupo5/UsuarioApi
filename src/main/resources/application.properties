# ======================================================
# ? CONFIGURACIÓN GENERAL
# ======================================================
spring.application.name=usuario-microservice
server.address=0.0.0.0
server.port=8082

spring.cloud.config.enabled=false
spring.cloud.compatibility-verifier.enabled=false

# ======================================================
# ? EUREKA CONFIG
# ======================================================
# ? Local
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

# ? Nube
# eureka.client.service-url.defaultZone=https://eureka-server-1jnn.onrender.com/eureka/

eureka.instance.instance-id=${spring.application.name}:${random.uuid}
eureka.instance.prefer-ip-address=true

eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.client.registry-fetch-interval-seconds=30
eureka.client.instance-info-replication-interval-seconds=30
eureka.client.initial-instance-info-replication-interval-seconds=40
eureka.instance.lease-renewal-interval-in-seconds=30
eureka.instance.lease-expiration-duration-in-seconds=90
eureka.instance.metadata-map.startup=${random.int}

# ======================================================
# ?? BASE DE DATOS
# ======================================================
# ? Conexión local (MariaDB)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://192.168.82.34:3306/UsuarioBD?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=json6033

# ? Conexión nube (Aiven)
# spring.datasource.url=jdbc:mysql://usuarioapi-1b744e33-usuarioapi-976b.i.aivencloud.com:11023/defaultdb?ssl-mode=REQUIRED
# spring.datasource.username=avnadmin
# spring.datasource.password=AVNS_NwbYcoKzY7gA_KiQVaW

# ======================================================
# ?? JPA / HIBERNATE
# ======================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# ======================================================
# ? JWT SECURITY
# ======================================================
security.jwt.secret-key=Y/I5T3T1aSOP+BezsicbSiUtzIZWxdcOVnylSYlJl/H22uPQID8VGjfzc5U5cVhKA6qm17V2yMP/mlrlTIkb+g==

# ======================================================
# ? HEALTH CHECKS
# ======================================================
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
management.health.eureka.enabled=true

# ======================================================
# ? CONFIG EXTRAS
# ======================================================
spring.cloud.discovery.enabled=true
spring.cloud.service-registry.auto-registration.enabled=true

# ======================================================
# ? LOGGING
# ======================================================
logging.level.org.springframework.security=WARN
logging.level.org.esfe.UsuarioApi.seguridad=INFO
logging.level.com.netflix.eureka=INFO
logging.level.com.netflix.discovery=INFO
